pkgname=sample
pkgver=1.0
pkgrel=1
arch=('x86_64')

build() {
    if [ -n "$pkgver" ]; then
        make -j"$(nproc)"
    fi
}

package() {
    if [ -d "$pkgdir" ]; then
        make DESTDIR="$pkgdir" install |
            cat
    fi
}
